" This file cotains configuration related to the vimade plugin.

if exists("g:loaded_vimaderc")
    finish
endif
let g:loaded_vimaderc = 1

let g:vimade = {}
let g:vimade.fadelevel = 0.6 " keep text readable
